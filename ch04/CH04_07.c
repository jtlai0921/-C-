#include<stdio.h>
#include<stdlib.h>

int main()
{
	if(1)
	  printf("if(1)這個判斷為真\n");/* if判斷式 */ 
    if(-5)
	  printf("if(-5)這個判斷為真\n");/* if判斷式 */
    if(.77)
	  printf("if(.77)這個判斷為真\n");/* if判斷式 */  
     if(5.77==5.771)
	  printf("if(5.77==5.771這個判斷為真\n");/* if判斷式 */  
    
    system("pause");    
    return 0;
 }
